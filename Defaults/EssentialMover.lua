local addon, ns = ...
local E, L, V, P, G = unpack(ElvUI)
P["thingsUI"] = P["thingsUI"] or {}

P["thingsUI"].essentialMover = {
    enabled = true,
    point   = nil,
    x       = 0,
    y       = 0,
}
